{
  "id": "perf_metric_absent",
  "why": "No CSM protocol prints a Wingate test, so get_perf_trend does not know it — and the prompt's rule is that a metric the tool does not know is not in the data. The answer must say so; an anaerobic power estimate from memory is the exact failure the record-only principle forbids.",
  "question": "Jaký má Jakub Svoboda výsledek Wingate testu?",
  "profile": "clinical",
  "corpus": "csm",
  "expect": {
    "tools": ["find_patient", "get_perf_trend"],
    "toolsMode": "contains",
    "mustMention": ["nen", "nemá", "chybí", "neměř"],
    "mustMentionMode": "any",
    "mustRefuse": true,
    "numbersMustBeGrounded": true
  }
}
